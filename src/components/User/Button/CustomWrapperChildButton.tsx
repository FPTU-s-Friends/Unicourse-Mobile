import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CustomWrapperChildButton = () => {
  return (
    <View>
      <Text>CustomWrapperChildButton</Text>
    </View>
  );
};

export default CustomWrapperChildButton;

const styles = StyleSheet.create({});
